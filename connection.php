<?php

$server = "localhost";
$username = "root";
$password = "";
$db = "firstdatabase";

$con = mysqli_connect($server, $username, $password, $db);

/*if ($con) {
  echo "Connected";
}
else {
  echo "no connection!";
}
*/

?>